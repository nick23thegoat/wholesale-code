# The engine runs on the Python standard library alone (Python 3.9+).
# There is nothing to install to analyze deals or to run a hunt:
#
#     python -m wholesale_engine.main --sample
#
# ---------------------------------------------------------------------
# The web dashboard is the ONE thing with a real dependency.
# ---------------------------------------------------------------------
# Needed only for `python -m wholesale_engine.web`. The CLI does not import
# it and does not need it installed.
#
#     pip install -r wholesale_engine/requirements.txt
#
# Bounded to the major version rather than pinned exactly: a patch release
# should reach a server without a code change, a major one should not.
# Tested against 3.1.3.
Flask>=3.0,<4.0

# Production WSGI server for the VPS deployment. Pure Python, no build
# toolchain. Flask's own server is for development only and must not serve
# the dashboard — see deploy/README.md.
gunicorn>=23.0,<27.0

# Optional development tooling:
pytest>=7.0          # nicer test runner; `python -m unittest` works without it
# mypy>=1.0          # static type checking
# ruff>=0.1          # linting / formatting
#
# Reserved for V2 integrations (property data, comps, county records, CRM).
# Deliberately NOT installed yet — V1 has no external data access by design.
# requests>=2.31
# pydantic>=2.0
# gspread>=6.0
